name: CI Pipeline (Self-hosted)

on:
    workflow_dispatch:
        inputs:
            module:
                description: "Оберіть модуль (або 'all')"
                required: true
                default: "all"
                type: choice
                options:
                    - all
                    - get_data
                    - sample_analysis

jobs:
    run-modules: # Назва завдання
        runs-on: [self-hosted, windows]

        strategy:
            fail-fast: false
            max-parallel: 1 # Щоб уникнути конфліктів доступу до файлів на Windows
            matrix:
                include:
                    - name: get_data
                      path: scripts\get_data.py
                    - name: sample_analysis
                      path: src\sample_analysis.py

        steps: # Тепер steps чітко всередині run-modules
            - name: Перевірка умов запуску
              run: |
                  if ("${{ github.event_name }}" -ne "workflow_dispatch" -or "${{ github.event.inputs.module }}" -eq "all" -or "${{ github.event.inputs.module }}" -eq "${{ matrix.name }}") {
                    Add-Content -Path $env:GITHUB_ENV -Value "SHOULD_RUN=true"
                  } else {
                    Add-Content -Path $env:GITHUB_ENV -Value "SHOULD_RUN=false"
                  }

            - name: Завантаження коду (Локальний метод)
              if: env.SHOULD_RUN == 'true'
              run: |
                  if (Test-Path .git) {
                    git fetch origin main
                    git reset --hard origin/main
                  } else {
                    git clone https://github.com/kovalets-vlad/open-data-ai-analytics.git .
                  }

            - name: Налаштування Python
              if: env.SHOULD_RUN == 'true'
              run: python --version

            - name: Встановлення залежностей
              if: env.SHOULD_RUN == 'true'
              run: |
                  python -m pip install --upgrade pip
                  if (Test-Path requirements.txt) { pip install -r requirements.txt }

            - name: Підготовка даних (тільки для модуля аналізу)
              if: env.SHOULD_RUN == 'true'
              run: |
                  if ("${{ matrix.name }}" -eq "sample_analysis") {
                    python scripts\get_data.py
                  }

            - name: Запуск модуля
              if: env.SHOULD_RUN == 'true'
              env:
                  PYTHONIOENCODING: "utf-8"
              run: |
                  if (-not (Test-Path "artifacts/${{ matrix.name }}")) { New-Item -ItemType Directory -Path "artifacts/${{ matrix.name }}" -Force }
                  python "${{ matrix.path }}"

            - name: Збереження результатів (Артефакти)
              if: env.SHOULD_RUN == 'true'
              uses: actions/upload-artifact@v4
              with:
                  name: local-results-${{ matrix.name }}
                  path: artifacts\${{ matrix.name }}
