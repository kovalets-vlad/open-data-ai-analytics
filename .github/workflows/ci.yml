name: Docker CI Pipeline

on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]

jobs:
    docker-test:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Prepare raw data
              run: |
                  mkdir -p data
                  if [ -f scripts/get_data.py ]; then python scripts/get_data.py; fi

            - name: Create dummy .env file
              run: |
                  echo "POSTGRES_USER=postgres" >> .env
                  echo "POSTGRES_PASSWORD=admin" >> .env
                  echo "POSTGRES_DB=equipment_db" >> .env
                  echo "POSTGRES_HOST=db" >> .env
                  echo "POSTGRES_PORT=5432" >> .env
                  echo "DATABASE_URL=postgresql://postgres:admin@db:5432/equipment_db" >> .env

            - name: Run Data Pipeline via Docker
              run: |
                  docker compose up --build data_load data_quality_analysis data_research visualization --abort-on-container-exit

            - name: Extract artifacts directly from containers
              run: |
                  mkdir -p final_results/reports
                  mkdir -p final_results/plots

                  ID_QUALITY=$(docker compose ps -qa data_quality_analysis)
                  ID_RESEARCH=$(docker compose ps -qa data_research)
                  ID_VISUAL=$(docker compose ps -qa visualization)

                  # Копіюємо конкретні файли, а не папки (це надійніше)
                  echo "Копіювання звітів..."
                  docker cp ${ID_QUALITY}:/app/reports/quality_report.json ./final_results/reports/ || echo "Quality report not found"
                  docker cp ${ID_RESEARCH}:/app/reports/research_report.json ./final_results/reports/ || echo "Research report not found"

                  echo "Копіювання графіків..."
                  # Копіюємо весь вміст папки plots
                  docker cp ${ID_VISUAL}:/app/plots/. ./final_results/plots/ || echo "Plots not found"

                  echo "--- ФІНАЛЬНА ПЕРЕВІРКА ВМІСТУ ПАПОК: ---"
                  find ./final_results -maxdepth 3 -not -path '*/.*'
                  echo "----------------------------------------"

            - name: Upload Artifacts
              uses: actions/upload-artifact@v4
              with:
                  name: analytics-docker-results
                  path: final_results/
