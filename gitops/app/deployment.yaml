apiVersion: apps/v1
kind: Deployment
metadata:
    name: web-deployment
    namespace: ai-analytics
spec:
    replicas: 1
    selector:
        matchLabels:
            app: web-app
    template:
        metadata:
            labels:
                app: web-app
        spec:
            containers:
                - name: web-container
                  image: k0va1ets/ai-analytics-web:v1
                  ports:
                      - containerPort: 80
